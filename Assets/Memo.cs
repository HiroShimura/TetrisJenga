using UnityEngine;

public class Memo : MonoBehaviour {

    /*
     * ミノを積んだ後の次のプレイヤーへの入れ替えにはコルーチンを使う
     * Rayを使ったミノの高さを調べる方法を用意し、ミノを積む際のカメラ移動とペナルティーで利用
     * ミノが落ちるかTableに触れたらGameOver
     * Tableに触れた場合はコルーチンを使ってGameOver（できればTETRIS99みたいなミノが灰色になるアニメーションが欲しいけど...）
     */

    // Start is called before the first frame update
    void Start() {

    }

    // Update is called once per frame
    void Update() {

    }
}
